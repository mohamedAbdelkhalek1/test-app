<odoo>

    <record model="ir.actions.act_window" id="act_session_graph">
      <field name="name">Attendees by course</field>
      <field name="res_model">test_app.session</field>
      <field name="view_mode">graph</field>
        <field name="view_id" ref="test_app.session_graph"/>
    </record>

    <record model="ir.actions.act_window" id="act_session_calendar">
      <field name="name">Sessions</field>
      <field name="res_model">test_app.session</field>
      <field name="view_mode">calendar</field>
        <field name="view_id" ref="test_app.session_calendar"/>
    </record>

    <record model="ir.actions.act_window" id="act_course_list">
      <field name="name">Courses</field>
      <field name="res_model">test_app.course</field>
      <field name="view_mode">tree,form</field>
<!--        <field name="view_id" ref="test_app.test_app_course_list"/>-->
    </record>


    <record model="ir.ui.view" id="board_session_view">
      <field name="name">session dashboard form</field>
      <field name="model">board.board</field>
      <field name="type">form</field>
      <field name="arch" type="xml">
        <form string="session dashboard">
          <board style="2-1">
            <column>
              <action string="Attendees by course" name="%(act_session_graph)d" height="150" width="510"/>
              <action string="Sessions" name="%(act_session_calendar)d"/>
            </column>
            <column>
              <action string="Courses" name="%(act_course_list)d"/>
            </column>
          </board>
        </form>
      </field>
    </record>

    <record model="ir.actions.act_window" id="open_board_session">
      <field name="name">session dashboard</field>
      <field name="res_model">board.board</field>
      <field name="view_mode">form</field>
      <field name="usage">menu</field>
        <field name="view_id" ref="board_session_view"/>
    </record>




    <menuitem name="Dashboards" id="test_app.dashboard" parent="test_app.menu_root"/>

    <menuitem name="session dashboard" parent="test_app.dashboard"
              action="open_board_session" sequence="1" id="menu_board_session"/>

</odoo>